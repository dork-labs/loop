---
title: CLI Reference
description: Complete command reference for the looped CLI tool. Install, configure, and use the Loop CLI to manage issues, signals, triage, templates, dispatch, and system status.
---

# CLI Reference

The `looped` CLI provides full command-line access to the Loop API. It is a thin HTTP client that covers issue management, signal submission, triage, prompt templates, dispatch, and system status.

## Installation

```bash
# Install globally
npm install -g looped

# Or run directly with npx
npx looped --help
```

## Configuration

Before using the CLI, configure your API endpoint and authentication token:

```bash
looped config set url https://api.looped.me
looped config set token loop_your_api_key_here
```

Configuration is stored in `~/.loop/config.json` with restricted file permissions (`0600`).

### Config commands

| Command | Description |
|---------|-------------|
| `looped config set <key> <value>` | Set `url` or `token` |
| `looped config get <key>` | Print a config value |
| `looped config list` | Print all config (token masked) |

### Resolution order

Configuration values resolve in this order (highest priority first):

1. CLI flags (`--api-url`, `--token`)
2. Environment variables (`LOOP_API_URL`, `LOOP_API_TOKEN`)
3. Config file (`~/.loop/config.json`)

## Global options

Every command supports these flags:

| Flag | Description |
|------|-------------|
| `--json` | Output raw JSON instead of formatted tables |
| `--api-url <url>` | Override API URL for this invocation |
| `--token <token>` | Override auth token for this invocation |
| `--version` | Print CLI version |
| `--help` | Show help for a command |

## Commands

| Command | Description |
|---------|-------------|
| [`looped list`](/docs/cli/issues) | List issues with filters |
| [`looped show <id>`](/docs/cli/issues) | Show full issue detail |
| [`looped create <title>`](/docs/cli/issues) | Create a new issue |
| [`looped comment <id> <body>`](/docs/cli/issues) | Add a comment to an issue |
| [`looped signal <message>`](/docs/cli/signals) | Submit a manual signal |
| [`looped triage`](/docs/cli/triage) | View and manage the triage queue |
| [`looped templates`](/docs/cli/templates) | Manage prompt templates and versions |
| [`looped next`](/docs/cli/dispatch) | Preview the dispatch queue |
| [`looped dispatch <id>`](/docs/cli/dispatch) | Force-claim an issue |
| [`looped status`](/docs/cli/status) | Show system health overview |

## Interactive vs non-interactive

The CLI detects whether it is running in a terminal (TTY) or a piped/CI environment and adjusts its behavior accordingly.

| Context | Behavior |
|---------|----------|
| TTY (terminal) | Colored output, interactive prompts, spinners |
| Piped / CI | Plain JSON-safe output, no prompts (uses defaults), no spinners |

## Environment variables

| Variable | Description |
|----------|-------------|
| `LOOP_API_URL` | API base URL |
| `LOOP_API_TOKEN` | Bearer token for authentication |

Using environment variables is recommended for CI pipelines to avoid exposing tokens in shell history.
