# Loop Dashboard
# Copy to .env — works immediately for local dev: cp .env.example .env

# API base URL (defaults to http://localhost:5667 if not set)
VITE_API_URL=http://localhost:5667

# API Key — must match apps/api/.env LOOP_API_KEY
# Generate with: pnpm run generate-key
VITE_LOOP_API_KEY=loop-dev-api-key-insecure
